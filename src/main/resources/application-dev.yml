myapp:
  dict-path: config/user.dict
  config-path: config
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

spring:
  datasource:
    url: jdbc:h2:file:D:\DevSpace\DevelopSpace\segment\h2db
    username: sa
    password: password
    driverClassName: org.h2.Driver